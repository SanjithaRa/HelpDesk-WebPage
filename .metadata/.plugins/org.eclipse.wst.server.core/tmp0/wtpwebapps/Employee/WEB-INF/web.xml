<?xml version="1.0" encoding="UTF-8"?>

<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
		xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee 
		http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd" 
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
		version="3.1">
		 
		 <servlet>
			<servlet-name>Login</servlet-name>
			<servlet-class>appointment.LoginServlet</servlet-class> <!-- match the servlet -->
		</servlet>
		
		<servlet-mapping> <!-- path to the servlet -->
		 	<servlet-name>Login</servlet-name>
		 	<url-pattern>/login</url-pattern> <!-- action to the payment insert form -->
		 </servlet-mapping>
		 
		<!-- insert payment --> 
		<servlet>
			<servlet-name>EmployeeInsert</servlet-name>
			<servlet-class>Employee.EmployeeInsertServlet</servlet-class> <!-- match the servlet -->
		</servlet>
		
		<servlet-mapping> <!-- path to the servlet -->
		 	<servlet-name>EmployeeInsert</servlet-name>
		 	<url-pattern>/insert</url-pattern> <!-- action to the payment insert form -->
		 </servlet-mapping>
		 
		 
		 <servlet>
			<servlet-name>EmployeeView</servlet-name>
			<servlet-class>Employee.EmployeeView</servlet-class> <!-- match the servlet -->
		</servlet>
		
		<servlet-mapping> <!-- path to the servlet -->
		 	<servlet-name>EmployeeView</servlet-name>
		 	<url-pattern>/view</url-pattern> <!-- action to the view form -->
		 </servlet-mapping>
		 
		 
		 <servlet>
			<servlet-name>updateEmployee</servlet-name>
			<servlet-class>Employee.UpdateEmpServlet</servlet-class> <!-- match the servlet -->
		</servlet>

		<servlet-mapping> <!-- path to the servlet -->
			<servlet-name>updateEmployee</servlet-name>
			<url-pattern>/update</url-pattern> <!-- action to the update form -->
		</servlet-mapping>
		
		
		<servlet>
			<servlet-name>deleteEmployee</servlet-name>
			<servlet-class>Employee.deleteEmpServlet</servlet-class> <!-- match the servlet -->
		</servlet>

		<servlet-mapping> <!-- path to the servlet -->
			<servlet-name>deleteEmployee</servlet-name>
			<url-pattern>/delete</url-pattern> <!-- action to the delete form -->
		</servlet-mapping>
		
		<!-- Sanduni Crud -->
		
				<!-- insert appointment --> 
		<servlet>
			<servlet-name>AppointmentInsert</servlet-name>
			<servlet-class>appointment.AppinsertSevlet</servlet-class> <!-- match the servlet -->
		</servlet>
		
		<servlet-mapping> <!-- path to the servlet -->
		 	<servlet-name>AppointmentInsert</servlet-name>
		 	<url-pattern>/insertapp</url-pattern> <!-- action to the payment insert form -->
		 </servlet-mapping>
		 
		 
		 <servlet>
			<servlet-name>AppointmentView</servlet-name>
			<servlet-class>appointment.AppViewServlet</servlet-class> <!-- match the servlet -->
		</servlet>
		
		<servlet-mapping> <!-- path to the servlet -->
		 	<servlet-name>AppointmentView</servlet-name>
		 	<url-pattern>/viewapp</url-pattern> <!-- action to the view form -->
		 </servlet-mapping>
		 
		 
		 <servlet>
			<servlet-name>updateAppointment</servlet-name>
			<servlet-class>appointment.UpdateAppSevlet</servlet-class> <!-- match the servlet -->
		</servlet>

		<servlet-mapping> <!-- path to the servlet -->
			<servlet-name>updateAppointment</servlet-name>
			<url-pattern>/updateapp</url-pattern> <!-- action to the update form -->
		</servlet-mapping>
		
		<!-- delete appointment -->
		
		<servlet>
			<servlet-name>deleteAppointment</servlet-name>
			<servlet-class>appointment.AppDeleteServlet</servlet-class> <!-- match the servlet -->
		</servlet>

		<servlet-mapping> <!-- path to the servlet -->
			<servlet-name>deleteAppointment</servlet-name>
			<url-pattern>/deleteapp</url-pattern> <!-- action to the delete form -->
		</servlet-mapping>
		
		<!-- minuli's crud --> 
		<!-- insert ticket --> 
		<servlet>
			<servlet-name>TicketInsert</servlet-name>
			<servlet-class>ticket.TicketinsertServlet</servlet-class> <!-- match the servlet -->
		</servlet>
		
		<servlet-mapping> <!-- path to the servlet -->
		 	<servlet-name>TicketInsert</servlet-name>
		 	<url-pattern>/insertticket</url-pattern> <!-- action to the payment insert form -->
		 </servlet-mapping>
		 
		 <servlet>
			<servlet-name>TicketView</servlet-name>
			<servlet-class>ticket.TicketViewServlet</servlet-class> <!-- match the servlet -->
		</servlet>
		
		<servlet-mapping> <!-- path to the servlet -->
		 	<servlet-name>TicketView</servlet-name>
		 	<url-pattern>/viewticket</url-pattern> <!-- action to the view form -->
		 </servlet-mapping>
		 
		 <servlet>
			<servlet-name>updateTicket</servlet-name>
			<servlet-class>ticket.UpdateTicketServlet</servlet-class> <!-- match the servlet -->
		</servlet>

		<servlet-mapping> <!-- path to the servlet -->
			<servlet-name>updateTicket</servlet-name>
			<url-pattern>/updateticket</url-pattern> <!-- action to the update form -->
		</servlet-mapping>
		
		<servlet>
			<servlet-name>deleteTicket</servlet-name>
			<servlet-class>ticket.ticketDeleteServlet</servlet-class> <!-- match the servlet -->
		</servlet>

		<servlet-mapping> <!-- path to the servlet -->
			<servlet-name>deleteTicket</servlet-name>
			<url-pattern>/deleteticket</url-pattern> <!-- action to the delete form -->
		</servlet-mapping>
		
		<!-- avishka's crud--> 
		<!-- insert feedback --> 
		<servlet>
			<servlet-name>feedbackInsert</servlet-name>
			<servlet-class>feedback.FeedbackInsertServlet</servlet-class> <!-- match the servlet -->
		</servlet>
		
		<servlet-mapping> <!-- path to the servlet -->
		 	<servlet-name>feedbackInsert</servlet-name>
		 	<url-pattern>/insertfeed</url-pattern> <!-- action to the payment insert form -->
		 </servlet-mapping>
		 
		 <servlet>
			<servlet-name>FeebackView</servlet-name>
			<servlet-class>feedback.FeedbackViewServlet</servlet-class> <!-- match the servlet -->
		</servlet>
		
		<servlet-mapping> <!-- path to the servlet -->
		 	<servlet-name>FeebackView</servlet-name>
		 	<url-pattern>/viewfeed</url-pattern> <!-- action to the view form -->
		 </servlet-mapping>
		 
		 <servlet>
			<servlet-name>updateFeedback</servlet-name>
			<servlet-class>feedback.updateFeedbackServlet</servlet-class> <!-- match the servlet -->
		</servlet>

		<servlet-mapping> <!-- path to the servlet -->
			<servlet-name>updateFeedback</servlet-name>
			<url-pattern>/updatefeed</url-pattern> <!-- action to the update form -->
		</servlet-mapping>
		
		
		
		<servlet>
			<servlet-name>deleteFeedback</servlet-name>
			<servlet-class>feedback.FeedbackDeleteServlet</servlet-class> <!-- match the servlet -->
		</servlet>

		<servlet-mapping> <!-- path to the servlet -->
			<servlet-name>deleteFeedback</servlet-name>
			<url-pattern>/deletefeed</url-pattern> <!-- action to the delete form -->
		</servlet-mapping>
		
		
		 </web-app>